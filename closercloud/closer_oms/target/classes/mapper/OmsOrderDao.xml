<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.closer.oms.dao">

    <select id="selectMyPage" resultType="cn.closer.oms.beans.OmsOrder">
        SELECT * FROM oms_order WHERE state=#{state}
    </select>

</mapper>
